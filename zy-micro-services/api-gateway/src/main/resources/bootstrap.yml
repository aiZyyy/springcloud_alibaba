spring:
  application:
    name: @project.artifactId@
  profiles: local
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: ${SPRING_CLOUD_NACOS_URI:http://localhost:8848}
      config:
        server-addr: ${SPRING_CLOUD_NACOS_URI:http://localhost:8848}
        prefix: ${spring.application.name}
        file-extension: yaml
        group: DEFAULT_GROUP
        access-key: 89357605-6ea1-4627-a85b-4c2069d96474
        shared-configs:
          dataId: application.yaml
          group: DEFAULT_GROUP
          refresh: true